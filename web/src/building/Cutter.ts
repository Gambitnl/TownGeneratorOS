import { Polygon } from '../geom/Polygon';
import { Point } from '../geom/Point';

export class Cutter {
  public static bisect(p: Polygon, v: Point, ratio: number, b: number, alley: number): Polygon[] {
    // Placeholder
    return [];
  }
}