import { Point } from './Point';

export class GeomUtils {
  public static distance2line(x1: number, y1: number, dx: number, dy: number, x: number, y: number): number {
    // Placeholder
    return 0;
  }

  public static interpolate(p0: Point, p1: Point, ratio: number): Point {
    // Placeholder
    return new Point(0, 0);
  }

  public static scalar(x1: number, y1: number, x2: number, y2: number): number {
    // Placeholder
    return 0;
  }
}